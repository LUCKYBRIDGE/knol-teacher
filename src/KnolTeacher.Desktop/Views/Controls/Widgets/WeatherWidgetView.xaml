<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.WeatherWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Region Select & Refresh Bar -->
        <Border Grid.Row="0" Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Padding="8,6" Margin="0,0,0,8">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right"
                        Content="🔄 지금 새로고침"
                        Click="BtnRefresh_Click"
                        Height="28"
                        Padding="8,0"
                        Background="#1E293B"
                        Foreground="#7DD3FC"
                        BorderBrush="#475569"
                        BorderThickness="1"
                        FontSize="10.5"
                        FontWeight="Bold"
                        Cursor="Hand"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📍" FontSize="12" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <!-- Use an explicitly light field with dark text. The platform ComboBox
                         template can otherwise render a light selection field with white text. -->
                    <ComboBox x:Name="CbRegion"
                              Height="28"
                              MinWidth="150"
                              MaxWidth="240"
                              FontSize="11"
                              Background="#F8FAFC"
                              Foreground="#0F172A"
                              BorderBrush="#CBD5E1"
                              Padding="7,1"
                              VerticalContentAlignment="Center"
                              SelectionChanged="CbRegion_SelectionChanged">
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Background" Value="#F8FAFC"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                                <Setter Property="Padding" Value="7,4"/>
                                <Style.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter Property="Background" Value="#DBEAFE"/>
                                        <Setter Property="Foreground" Value="#0F172A"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#BAE6FD"/>
                                        <Setter Property="Foreground" Value="#0C4A6E"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.ItemContainerStyle>
                    </ComboBox>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Weather & Temp Hero -->
        <Border Grid.Row="1" Background="#0F172A" CornerRadius="10" BorderBrush="#334155" BorderThickness="1" Padding="14,10" Margin="0,0,0,8">
            <Viewbox Stretch="Uniform">
                <Grid Margin="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtWeatherIcon" Text="☀️" FontSize="52" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="TxtTemperature" Text="--°C" FontFamily="Consolas, Malgun Gothic" FontSize="36" FontWeight="Bold" Foreground="White"/>
                            <TextBlock x:Name="TxtWeatherDesc" Text="날씨 정보를 불러오는 중입니다" FontSize="12" Foreground="#CBD5E1"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                        <TextBlock x:Name="TxtHumidity" Text="💧 습도 --%" FontSize="11.5" Foreground="#7DD3FC" Margin="0,0,12,0"/>
                        <TextBlock x:Name="TxtWind" Text="💨 풍속 --m/s" FontSize="11.5" Foreground="#E2E8F0"/>
                    </StackPanel>
                </Grid>
            </Viewbox>
        </Border>

        <!-- Dust & Outdoor Activity Guide -->
        <StackPanel Grid.Row="2">
            <Border Background="#0F172A" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Padding="10,7" Margin="0,0,0,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="미세(PM10): " FontSize="11" Foreground="#CBD5E1"/>
                        <Border x:Name="BdPm10" Background="#14532D" CornerRadius="4" Padding="5,1">
                            <TextBlock x:Name="TxtPm10" Text="--" FontSize="10.5" FontWeight="Bold" Foreground="#86EFAC"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="초미세(PM2.5): " FontSize="11" Foreground="#CBD5E1"/>
                        <Border x:Name="BdPm25" Background="#14532D" CornerRadius="4" Padding="5,1">
                            <TextBlock x:Name="TxtPm25" Text="--" FontSize="10.5" FontWeight="Bold" Foreground="#86EFAC"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <Border x:Name="BdOutdoorGuide" Background="#14532D" BorderBrush="#22C55E" BorderThickness="1" CornerRadius="8" Padding="10,7">
                <TextBlock x:Name="TxtOutdoorGuide" Text="🏃 실외활동 정보를 불러오는 중입니다" FontSize="11.5" FontWeight="Bold" Foreground="#86EFAC" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
            </Border>
        </StackPanel>

        <TextBlock Grid.Row="3"
                   x:Name="TxtUpdatedTime"
                   Text="열 때 즉시 동기화 · 열린 동안 15분마다 자동 갱신"
                   HorizontalAlignment="Right"
                   Margin="0,7,2,0"
                   Foreground="#94A3B8"
                   FontSize="10"/>
    </Grid>
</UserControl>
