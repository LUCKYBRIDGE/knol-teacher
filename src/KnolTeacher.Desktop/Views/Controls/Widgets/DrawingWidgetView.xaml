<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.DrawingWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="VisibleBoardRadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Grid Width="18" Height="18" Margin="0,0,5,0">
                                <Ellipse x:Name="Ring" Fill="#F8FAFC" Stroke="#94A3B8" StrokeThickness="1.5"/>
                                <Ellipse x:Name="Dot" Width="9" Height="9" Fill="#0284C7" Visibility="Collapsed"/>
                            </Grid>
                            <ContentPresenter ContentSource="Content" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Ring" Property="Stroke" Value="#7DD3FC"/>
                                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Ring" Property="Stroke" Value="#38BDF8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0F172A" CornerRadius="7" BorderBrush="#334155" BorderThickness="1" Padding="6,4" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <WrapPanel Grid.Column="0" VerticalAlignment="Center">
                    <Button x:Name="BtnModeGreen" Content="🏫 칠판" Click="BtnModeGreen_Click" Height="28" Padding="8,0" Margin="0,0,4,3" Background="#1B4332" Foreground="#DCFCE7" FontSize="10.5" FontWeight="Bold"/>
                    <Button x:Name="BtnModeWhite" Content="📋 화이트" Click="BtnModeWhite_Click" Height="28" Padding="8,0" Margin="0,0,8,3" Background="#F8F9FA" Foreground="#0F172A" FontSize="10.5" FontWeight="Bold"/>

                    <RadioButton x:Name="RbPen" Content="펜" IsChecked="True" Checked="RbPen_Checked" Style="{StaticResource VisibleBoardRadioStyle}" FontSize="10.5" Margin="0,0,8,3" VerticalAlignment="Center"/>
                    <RadioButton x:Name="RbEraser" Content="지우개" Checked="RbEraser_Checked" Style="{StaticResource VisibleBoardRadioStyle}" FontSize="10.5" Margin="0,0,10,3" VerticalAlignment="Center"/>

                    <Button Width="20" Height="20" Background="White" Click="BtnColor_Click" Tag="#FFFFFF" Margin="0,2,3,3" Cursor="Hand" BorderBrush="#94A3B8" BorderThickness="1" ToolTip="흰색"/>
                    <Button Width="20" Height="20" Background="#FDE047" Click="BtnColor_Click" Tag="#FDE047" Margin="0,2,3,3" Cursor="Hand" BorderThickness="0" ToolTip="노랑"/>
                    <Button Width="20" Height="20" Background="#F472B6" Click="BtnColor_Click" Tag="#F472B6" Margin="0,2,3,3" Cursor="Hand" BorderThickness="0" ToolTip="분홍"/>
                    <Button Width="20" Height="20" Background="#38BDF8" Click="BtnColor_Click" Tag="#38BDF8" Margin="0,2,6,3" Cursor="Hand" BorderThickness="0" ToolTip="하늘색"/>
                </WrapPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="↩" Click="BtnUndo_Click" Height="28" Padding="7,0" Margin="0,0,4,0" Background="#334155" Foreground="White" FontSize="10.5" ToolTip="실행 취소"/>
                    <Button Content="🗑" Click="BtnClear_Click" Height="28" Padding="7,0" Background="#DC2626" Foreground="White" FontSize="10.5" ToolTip="전체 지우기"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" x:Name="InkBorder" Background="#1B4332" CornerRadius="10" BorderBrush="#475569" BorderThickness="1" ClipToBounds="True">
            <InkCanvas x:Name="MiniInkCanvas" Background="Transparent"/>
        </Border>
    </Grid>
</UserControl>
