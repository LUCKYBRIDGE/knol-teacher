<UserControl x:Class="KnolTeacher.Desktop.Views.Controls.Widgets.QrWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Source URL/text. Typing is debounced so long links remain responsive. -->
        <Grid Grid.Row="0" Margin="0,0,0,7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TbWidgetUrl"
                     Grid.Column="0"
                     Height="36"
                     Padding="9,4"
                     VerticalContentAlignment="Center"
                     FontSize="12"
                     Background="#F8FAFC"
                     Foreground="#0F172A"
                     BorderBrush="#CBD5E1"
                     Text="https://pinky-ne.com/"
                     TextChanged="TbWidgetUrl_TextChanged"
                     ToolTip="웹 주소 또는 QR로 만들 텍스트를 입력하세요. 주소에 https://가 없으면 자동으로 보완합니다."/>
            <Button Grid.Column="1"
                    Content="↻ QR 갱신"
                    Click="BtnRefresh_Click"
                    Height="36"
                    Padding="10,0"
                    Margin="6,0,0,0"
                    Background="#334155"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="11"
                    FontWeight="Bold"
                    Cursor="Hand"/>
        </Grid>

        <!-- The exact normalized content used by the QR is always copyable as text. -->
        <Border Grid.Row="1"
                Background="#0F172A"
                BorderBrush="#334155"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8,7"
                Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="학생에게 전달할 링크"
                                   Foreground="#CBD5E1"
                                   FontSize="10.5"
                                   FontWeight="SemiBold"
                                   Margin="1,0,0,4"/>
                        <TextBox x:Name="TbShareLink"
                                 IsReadOnly="True"
                                 Height="32"
                                 Padding="8,3"
                                 VerticalContentAlignment="Center"
                                 FontSize="11.5"
                                 Background="#F8FAFC"
                                 Foreground="#0F172A"
                                 BorderBrush="#CBD5E1"
                                 HorizontalScrollBarVisibility="Auto"
                                 ToolTip="QR에 실제로 들어가는 내용입니다. 긴 주소도 복사 버튼으로 전체가 복사됩니다."/>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Content="🔗 링크 복사"
                            Click="BtnCopyLink_Click"
                            Height="32"
                            Padding="11,0"
                            VerticalAlignment="Bottom"
                            Background="#0284C7"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="11.5"
                            FontWeight="Bold"
                            Cursor="Hand"/>
                </Grid>
                <TextBlock x:Name="TxtLinkHint"
                           Grid.Row="1"
                           Text="QR을 찍기 어려운 학생에게는 이 링크를 그대로 복사해 전달할 수 있습니다."
                           Margin="2,6,0,0"
                           Foreground="#94A3B8"
                           FontSize="10"
                           TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <!-- Scanner-friendly white quiet area -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                BorderBrush="#CBD5E1"
                BorderThickness="1"
                Margin="0,0,0,8"
                Padding="16">
            <Grid>
                <Image x:Name="ImgWidgetQr"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                <TextBlock x:Name="TxtQrStatus"
                           Text="QR 준비 중..."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Bottom"
                           Margin="8"
                           Padding="8,3"
                           FontSize="10.5"
                           Foreground="#475569"
                           Background="#E6F8FAFC"/>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="📋 QR 이미지 복사"
                    Click="BtnCopyQrImage_Click"
                    Height="34"
                    Margin="0,0,4,0"
                    Background="#334155"
                    Foreground="White"
                    FontSize="11.5"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
            <Button Grid.Column="1"
                    Content="💾 PNG 저장"
                    Click="BtnSavePng_Click"
                    Height="34"
                    Margin="4,0,0,0"
                    Background="#334155"
                    Foreground="White"
                    FontSize="11.5"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
            </Button>
        </Grid>

        <Button Grid.Row="4"
                Content="🔍 교실 화면에 크게 보기"
                Click="BtnZoom_Click"
                Height="36"
                Background="#0369A1"
                Foreground="White"
                FontSize="11.5"
                FontWeight="Bold"
                BorderThickness="0"
                Cursor="Hand">
            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
        </Button>
    </Grid>
</UserControl>
